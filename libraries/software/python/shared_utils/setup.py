from setuptools import setup, find_packages

setup(
    name="shared_utils",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    include_package_data=True,
    description="A shared utilities package for Python projects",
    author="Your Name",
    author_email="your.email@example.com",  # Optional, if you want to host it on GitHub
)

